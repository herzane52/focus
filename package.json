{
  "name": "focus-app",
  "private": true,
  "version": "1.3.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:app": "npm run tauri -- dev",
    "dev:test": "FOCUS_ENV=test npm run tauri -- dev",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "build:frontend": "npm run build",
    "build:prep": "npm run build:frontend && CARGO_TARGET_DIR=$PWD/build/target cargo build --release --manifest-path src-tauri/Cargo.toml",
    "build:deb": "CARGO_TARGET_DIR=$PWD/build/target npm run tauri -- build --bundles deb && mkdir -p build/packages && cp build/target/release/bundle/deb/*.deb build/packages/",
    "build:appimage": "CARGO_TARGET_DIR=$PWD/build/target APPIMAGE_EXTRACT_AND_RUN=1 NO_STRIP=1 npm run tauri -- build --bundles appimage && mkdir -p build/packages && cp build/target/release/bundle/appimage/*.AppImage build/packages/",
    "build:arch": "BUILDDIR=$PWD/build/makepkg makepkg -sf --noconfirm && mkdir -p build/packages && mv *.pkg.tar.zst build/packages/",
    "build:all": "npm run build:prep && CARGO_TARGET_DIR=$PWD/build/target APPIMAGE_EXTRACT_AND_RUN=1 NO_STRIP=1 npm run tauri -- build --bundles deb appimage && mkdir -p build/bin build/packages && cp build/target/release/focus-app build/bin/ && cp build/target/release/bundle/deb/*.deb build/packages/ && cp build/target/release/bundle/appimage/*.AppImage build/packages/ && npm run build:arch"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-fs": "^2.4.5",
    "@tauri-apps/plugin-opener": "^2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-react": "^4.6.0",
    "autoprefixer": "^10.4.24",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18",
    "vite": "^7.0.4"
  }
}
